/**
 * useInventory — re-exported from InventoryContext for backwards compatibility.
 * All pages that import from hooks/useInventory.js continue to work unchanged.
 */
export { useInventory } from '../context/InventoryContext'
